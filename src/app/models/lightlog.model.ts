//Model needs to be the EXACT
//same name as the data you are recieving to work.
export interface LightLog{
    id: string;
    hours: string;
    minutes: string;
    seconds: string;
    dateSent: string;
}